# Prettier configuration for Vim ALE and Sublime Text
# Handles JSON (4 spaces) and Sublime settings (4 spaces, no trailing commas)
# Note: Markdown formatting is handled by mdformat, not Prettier

tabWidth = 2

# Wrap lines at 100 characters
printWidth = 100

# Don't add semicolons in JavaScript
semi = false

# Single quotes disabled - using Prettier defaults
#singleQuote = true

# JSON-specific overrides (4 spaces for JSON files)
[[overrides]]
files = "*.json"
[overrides.options]
tabWidth = 4

# Sublime settings overrides (4 spaces, no trailing commas)
[[overrides]]
files = "*.sublime-settings"
[overrides.options]
tabWidth = 4
trailingComma = "none"
