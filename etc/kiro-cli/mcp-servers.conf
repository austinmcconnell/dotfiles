#!/bin/bash
# Shared MCP server definitions used by install and update scripts

# Node.js MCP servers (installed globally via npm)
NODE_MCP_SERVERS=(
    "@aashari/mcp-server-atlassian-jira"
    "kubernetes-mcp-server"
)

# Python MCP servers (cached via uvx)
PYTHON_MCP_SERVERS=(
    "mcp-server-git"
    "mcp-server-time"
)

# GitHub MCP Server (installed via go install)
GITHUB_MCP_SERVER_PKG="github.com/github/github-mcp-server/cmd/github-mcp-server@latest"
